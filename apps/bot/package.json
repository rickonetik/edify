{
  "name": "@tracked/bot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && pnpm start",
    "build": "tsc -p tsconfig.json",
    "start": "node --env-file-if-exists=../../.env dist/apps/bot/src/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tracked/shared": "workspace:*",
    "grammy": "^1.24.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30"
  }
}
