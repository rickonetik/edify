{
  "name": "tracked-lms",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "prepare": "husky",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "clean": "turbo run clean && rm -rf node_modules",
    "dev": "turbo run dev --parallel",
    "dev:webapp": "pnpm --filter @tracked/webapp dev",
    "dev:public": "node -e \"const net = require('net'); const checkPort = (port) => new Promise((resolve) => { const server = net.createServer(); server.listen(port, () => { server.close(() => resolve(true)); }); server.on('error', () => resolve(false)); }); checkPort(5173).then((isOpen) => { if (!isOpen) { console.log('⚠️  Port 5173 is not listening. Start webapp first:'); console.log('   pnpm dev:webapp'); process.exit(1); } console.log('✅ Port 5173 is listening'); console.log(''); console.log('📋 Next steps:'); console.log(''); console.log('1. Start ngrok tunnel:'); console.log('   ngrok http 5173'); console.log(''); console.log('2. Copy the public URL from ngrok (e.g., https://xxxx.ngrok-free.app)'); console.log(''); console.log('3. Add to .env:'); console.log('   TELEGRAM_WEBAPP_URL=https://xxxx.ngrok-free.app'); console.log(''); console.log('4. Start bot:'); console.log('   BOT_TOKEN=your_token pnpm --filter @tracked/bot start'); console.log(''); console.log('📖 Full runbook: docs/runbooks/telegram-dev.md'); });\""
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@typescript-eslint/eslint-plugin": "^8.46.4",
    "@typescript-eslint/parser": "^8.46.4",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-import": "^2.31.0",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-react-hooks": "^5.1.0",
    "globals": "^16.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.2.11",
    "prettier": "^3.4.2",
    "turbo": "^2.0.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.46.4"
  }
}
